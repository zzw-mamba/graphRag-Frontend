import { data } from "./16jsonData"

export const systems = data.systems
export const instances = data.instances
export const databases = data.databases
export const tables = data.tables
export const fields = data.fields
export const links = data.links